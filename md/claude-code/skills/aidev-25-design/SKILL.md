---
name: aidev-25-design
description: ［任意工程・末尾5／主トリガ:AI検知推奨 or ユーザー指定］AI開発ワークフローの design（詳細設計）工程。spec と plan の間で構造設計を固める。「設計を詰めたい」「design工程」などと言われたとき、または spec 終了時に複雑度が高いと検知され推奨されたときに使用する。
allowed-tools: [Bash, Read, Write, Edit, AskUserQuestion, Agent]
---

AI 開発ワークフローの **design（詳細設計）工程**（任意）。
spec（何を作るか）と plan（作業分解）の間で、**構造設計**を固める。
モジュール分割・インターフェース・データモデル・処理シーケンスなど、plan で分解する前提となる
設計を具体化する。

**開始前に共通プロトコル `../aidev-00-start/protocol.md` を読み、その規約に従うこと。**

## 位置づけ

- 任意工程（番号末尾5、spec と plan の間に差し込む）。小〜中規模なら spec＋plan で足りるため不要。
- 起動経路は2つ（protocol.md「4.5」）: ユーザー指定、または spec 終了時の AI検知＋推奨（複雑度が高い場合）。
- 設計検討が重い場合はサブエージェント委譲（protocol.md「2.6」）も可。

## 起動判定（検知ロジック）

spec 終了時（spec 工程の終了ゲート）に、以下の**いずれか1つ以上**に該当したら design を発火対象とする。

- 変更が**複数コンポーネント／モジュールにまたがる**。
- **アーキテクチャ判断**（新規の構造・パターン・責務分割の選択）が必要。
- **インターフェース／データモデルが複雑**（型・スキーマ・状態遷移の設計に踏み込む必要がある）。
- spec が**plan で直接分解するには粗い**（作業に落とす前に構造を固める必要がある）。

- **interactive**: 上記検知時、遷移ゲートに `承認して design(任意) を挟む`（推奨）を理由付きで加える。却下されれば plan へ直行。
- **autonomous**: 上記検知時、**推奨ではなく自動で実施**する（protocol.md「10.」）。いずれにも該当しなければスキップして plan へ進む。
- 小〜中規模で上記に当たらなければ design は不要（spec＋plan で足りる）。

## 前提

- `spec.md` が存在すること。無ければ実行を中止し、spec 工程を促す。

## 入力

- 対象フォルダの `spec.md`（必要に応じて `requirement.md` / `research.md`）。
- 既存コードの構造、PJ のアーキテクチャ規約。

## 出力

対象フォルダに `design.md` を生成する（plan の入力になる）。

## 手順

1. protocol.md「1. 対象作業の特定」に従い対象フォルダを確定。「2. 前提チェック」で `spec.md` を確認。
2. spec を実現する構造を設計する。典型的な観点:
   - コンポーネント / モジュール分割と責務
   - 主要インターフェース・型・データモデル
   - 処理フロー / 状態遷移 / シーケンス（必要なら）
   - 既存アーキテクチャとの整合、横断的関心事（エラー処理・ログ等）
3. 設計上の選択は理由とともに残す（採用案・退けた代替案）。重い検討は委譲してよい。
4. 下記テンプレートに沿って `design.md` を記述する。
   アーキテクチャ/コンポーネント・class・sequence・state は mermaid で図示する（protocol.md「9.」）。
5. protocol.md「3. 工程終了プロトコル」に従って終了する（次工程: `plan`）。

## design.md テンプレート

```markdown
# 設計: <タイトル>

## アーキテクチャ概要
<全体構造の要約。図や箇条書きで>

## コンポーネント / モジュール
- <名称>: <責務 / 依存>

## インターフェース / データモデル
- <主要な型・API・スキーマ>

## 処理フロー / シーケンス
- <重要な経路の流れ。状態遷移があれば>

## 設計判断
- <採用したアプローチと理由。退けた代替案>

## plan への申し送り
- <作業分解時に踏まえるべき分割単位・順序の示唆>
```

## 完了の目安

- spec を実装するための構造が、plan で作業分解できる粒度まで具体化している。
- 主要な設計判断が理由付きで残っている。
