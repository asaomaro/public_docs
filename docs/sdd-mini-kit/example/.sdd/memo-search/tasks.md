# memo-search タスク

- [ ] 1. 検索欄を一覧の上に置き、入力を useState で保持する（対象: src/App.jsx）
- [ ] 2. キーワードで本文を絞り込んでから一覧を描く（大文字小文字は無視）（対象: src/App.jsx）
- [ ] 3. 絞り込み結果が0件のとき「該当なし」を表示する（対象: src/App.jsx）

## 進め方
上から順に。1つ終えるごとにチェックを入れる。
実装が終わったら `/sdd-verify` で受け入れ条件を確かめる。
