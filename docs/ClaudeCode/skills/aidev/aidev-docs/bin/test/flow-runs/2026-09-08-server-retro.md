---
date: 2026-09-08
change: 他 PJ の retro（server）のハーネス提案 6 件を検証し、5 件を適用・1 件を見送る（2 件は主張を否定）
---

## 1. 文書は実態に追いついているか

**受け取った提案をそのまま入れない**——実物で確かめた。**6 件中 2 件で前提が事実と違った。**

| 提案 | 判定 | 根拠（実測） |
|---|---|---|
| H1 conv id の実在検査が無い | **事実** | 存在しない `[conv:agents!]` で `approve review` が無警告・exit 0 |
| H2 数え上げの陳腐化 | **事実だが見送り** | 検査は無い。対象の決め方が PJ ごとに違う（下記） |
| H3 backlog に 1 行足す口が無い | **事実** | 口は `new`/`archive`/`compact` の 3 つ |
| H4 metrics に空白区間の列が無い | **事実** | 列に相当するものが無い |
| H5 「最小の差分」が (a) にしかない | **前提は誤り** | `protocol-check.md:29` は `(a)`（:32）より前の**共通節**。`:7` が「規律は共通」と明言 |
| H6 `smoke` が本数を言わない | **誤り。真の欠陥は別** | 2 本以上なら出る。**コメントでリストが閉じる**のが原因 |

**H6 が一番の収穫**。報告された対処（本数を出す）は既に実装済みで、**症状を再現して初めて
真の原因が分かった**——`smokeCommands` のブロック内にコメントが 1 行あるとパーサがそこで閉じ、
2 本設定して 1 本しか走らない。そのとき本数は 1 なので**本数すら出ず**、`doctor` も `commands=1`。
**報告された対処ではなく、報告された症状を再現してから原因を直す。**

**H5 は前提が誤りでも substance は有効**だった。「規則そのものを書き直したら、書き直した規則を
新規実装として点検し直す」は未存在で、根拠の実測（書き直しの根拠に置いた前提のほうが誤っていて
退行を作った）も具体的。**前提が誤っていることと提案が無価値であることは別**。

**H2 の見送り理由を、監査に否定されて書き直した**。最初「6 件はすべて製品側の TypeScript で
aidev からは見えない」と書いたが、6 件目は `AGENTS.md`（`conventionsIndex` 未設定なら aidev も読む）。
**確かめた記録を書いている当の節で、確かめた側が事実を外していた**。正しい理由は
「**数え上げが載る場所が PJ ごとに違い、ハーネスが対象を決められない**」。

## 2. README のメンテナンス

- `aidev-docs/bin/README.md` — `backlog add` の行を新設、`metrics` に `idle_sec`、`approve` に conv id 検査。
- `aidev-00-start/protocol.md`「8.」 — **`idle_sec` を `work_sec` から引かない**を追記。
  唯一の禁止事項が `bin/README.md` の巨大な 1 セルにしか無く、retro を書く導線から辿り着けなかった（実走が実測）。
- `aidev-util-insights/SKILL.md` — **横断比較の当事者**に `idle_sec` を届けた。retro の課題が名指ししたのは
  まさに insights 側の比較で、**列を足しただけでは読み手に届いていなかった**（監査が実測）。
- `aidev-95-retro/SKILL.md` — `backlog add` の導線、`<file>` の選び方、不在時の手順、`--source` の書式。

## 3. 実走（サブエージェント）

テスト PJ で `requirements → design → tasks → coding → test → review → retro` を通し、
新設した 4 つを実際に使わせた。`verify --strict` exit 0。**must 2 件**が出た。

- **F1: 4 箇所目を落としていた。** コメント問題を sh 2 箇所と ps1 で直したが `smoke_orphan_lines()` が漏れ、
  `doctor` が「**ブロックの外に `- ` 行が 2 本ある（黙って無視されます）**」と `commands=2`（全部読めている）を
  **同じ出力で同時に**言った——直しようが無い、消せない WARN。ps1 には入っていたので**非対称**でもあった。
- **F2: 私が入れた検査が既存の穴を継いでいた。** タグ抽出が `- [` の行しか見ないので、
  **折り返した指摘の 2 行目のタグが完全に素通り**する。同じコードのコメントが「母集団が黙って欠けるのが害」と
  警告している、その害の別経路。指摘を「`- [` 行＋字下げした継続行」の論理行として扱い、
  `approve` 側と効果検証側（`cv_count_tags`）を**同じ絞り方に揃えた**（集計器が 2 つ食い違うのは protocol.md が
  警告している事故）。地の文を拾わない性質は維持。

ほか実走の指摘（すべて反映）: `help` の継続行が `add` の説明に読める位置／`--source` のパス形式が未記載／
`idle_sec` の「既定」が設定可能に読める（閾値は固定）／WARN に「本当に条項にすべきなら `convention new`」が無い。

### 独立監査（もう 1 本）

**must 2・should 7・nit 6**、すべて実測付き。**must の 1 件と should の大半が私の新機能の欠陥。**

- **[must] `backlog add` が末尾改行の無いファイルで行を連結した。** 既存項目の文言まで壊れ、
  新項目が `status` の未着手件数に乗らない——**この機能が塞ごうとしている事故そのもの**を無徴候で再現していた。
- **[should] 重複判定がファイル全体の部分一致で誤爆。** 既存項目の部分文字列・frontmatter の
  `backlog: <name>`・**消し込み済み `- [x]` の文言**まで拒否した。最後のは
  「同じ穴が再発したので再起票する」経路をそのまま塞ぐ。→ 未消化項目の本文と完全一致で見る。
- **[should] `--source` だけ改行検査から漏れていた。** 1 回の `add` で未着手が 2 件増える（偽項目の注入）。
- **[should] conv id の実在検査が厳しい id の形しか見ていなかった。**
  **`[conv:AGENTS.md!]`（retro 当事者が実際に書いた形）が素通り**——「語彙を発明しない」を破る
  書き方ほど id の形も外れるので、**厳しい形だけ見るといちばん見たいものが見えない**。
- **[should] `smoke` 自身が孤児行を言わなかった。** `doctor` だけが言うが、**GO/NO-GO のゲートは smoke の側**。
  retro の H6 が求めたのはこの位置だった。
- **[should] ps1 のパリティ検査が新設 4 機能に 1 本も無かった。** この改修自身が繰り返し
  「sh/ps1 の共有欠陥・非対称」を反省しているのに、検査が無いのは方針と矛盾。→ 4 機能とも追加。
- [nit] ps1 の `--source` エラー文言が共通ヘルパと違う／`DESIGN` の「3 箇所」が実際は 6 箇所／
  ts 逆行のアサートが無い——いずれも反映。

**指摘なしと確認された点**（監査の実測）: `[conv:-]` を実在しない扱いにしない／archive 済み条項は実在扱い／
条項ディレクトリが無い PJ でも落ちない／`idle_sec` は `work_sec` を書き換えていない／
境界（ちょうど 1800 秒）は数えない／特殊文字は sh/ps1 で同一に扱われる。

**範囲外として残した**（監査の nit）: 壊れた `ts` での sh/ps1 の乖離（既存の `epoch` の非対称。
`idle_sec` が 3 本目の被害列になった）／行末 `# …` がコマンドの一部のままである点／
`bl_add` が節を選べない点。

## この改修の教訓

**「受け取った提案を実物で確かめる」を実際にやると、2/6 で前提が外れた。**
しかも**確かめた記録を書いている当の節で、確かめた側（私）が事実を外した**（H2 の理由）。
検証は 1 回では閉じない——**実走と独立監査の 2 本がそれぞれ別の穴を突いた**（実走は
「4 箇所目の漏れ」と「継承した穴」、監査は「新機能自身の 3 つの欠陥」）。
