---
date: 2026-09-05
change: harnessRev の unknown フォールバック修正 / doccheck（上流文書の独立点検）と schema 11 / 3 ゲートの機械化（L8）
---

## 1. 文書は実態に追いついているか

**独立監査エージェント 1 本**（実装を正として文書を突き合わせ、変更は禁止）。**20 件**の指摘。
結論は当初 **No** で、追いついていなかったのは「新機能の説明」ではなく**周辺の既存記述**だった。

| 直したもの | 何が起きていたか |
|---|---|
| `protocol-check.md` の「使わない場面」 | 「自明な成果物」が同じ節の「`autonomous` では必須」と正面衝突。CLI は自明さを見ないので、45 行目を読んで省くと `--strict` で必ず落ちる |
| `protocol-autonomous.md`「安全弁（autonomous 必須）」 | **doccheck が無い**。付録表は autonomous の work でこれを読めと言うのに、必須事項一覧から schema 11 に到達できない |
| `protocol.md`「3.3」の見出し | 「独立検証（…**任意**）」のまま。正典側が実態と逆を言っていた |
| 起動経路の一覧（3 箇所） | 「spec・design の工程内」のまま。requirement / plan にも工程内ステップが増えたのに反映されず、**`autonomous` では承認ゲートが無い**ので「requirement と plan には起動経路が 1 つも無い」と読めた |
| `protocol-analysis.md` | 刻んだ `doc_check_*` の**読み方が無い**（taskcheck 側だけ詳細にある）。`--mode` を正直に刻む労力が回収されない |
| `aidev-docs/README.md` の `--strict` 列挙 / config キー | 上流点検と `maxDocCheckRounds` が抜け |
| `bin/README.md` の終了コード表・`limits` の下限列挙 | `doccheck` / `taskcheck` の 4 と `maxDocCheckRounds` の下限が抜け |
| subtask の扱い | **どこにも書かれていなかった**。実測すると子自身の `plan.md` だけが対象（上流3文書は親から継承するので子に md が無く、条件で自然に外れる） |
| 重複 | 「autonomous では必須」が 7 箇所に言い換えで散在。正典を `protocol-check.md`「(a)」に決め、SKILL は引き金と打つコマンドだけにした |

**L5 は同一行しか見ないので、言い換えた重複は検出されない**——ゲート 1 が人／エージェントの担当である理由がそのまま出た。

## 2. README のメンテナンス

- `bin/README.md`（CLI リファレンス）: `doccheck` 行・`limits` 行・設定表・終了コード表・schema 11 の履歴・**L8 の行**・「ハーネス改修の完了の定義（3 ゲート）」節。
- `aidev-docs/README.md`（概観）: 「ループの上限」表に `maxDocCheckRounds`、`autonomous` で必須である旨、`--strict` の列挙、`.aidev/` レイアウトの config キー。
- **概観に一度も出てこない機能は存在しないのと同じ**という前例（`limits`）があるので、概観への反映を先に済ませた。

## 3. 実走（サブエージェント）

**テスト PJ `dcpj`（Python の `tally` CLI）を新規に起こし、`--mode autonomous` で requirement → deliver を 1 本**。
上流4工程すべてで `doccheck`、coding の 4 タスクで `taskcheck`、上限超過・対欠落・不正 mode の異常系も実測。
着地: `feature/words` に 2 コミット、`pytest` 5 passed、`smoke` 2 本 pass、`verify --strict` 0。

**見つかった実バグ（すべて修正済み・回帰テスト追加）**

| # | 何が起きたか | なぜ静的検査で出なかったか |
|---|---|---|
| B-1 | **`aidev limits` が work 0 本の PJ で無言の exit 1**。`die` は exit するので `if resolve_work …` では受からず、`2>/dev/null` がメッセージまで消していた。**ps1 も同じ**（`try/catch` も `exit` は受からない）。しかも「導入直後に上限値を確かめたい」まさにその瞬間に効かない | テストが常に work のある PJ で回していた。両実装が同じ穴なのでパリティも緑 |
| A-2 | **点検を一度も打たずに `approve <phase> doc_check_mode=delegated` と書くだけで `verify --strict` を通せた**。散文は「手で渡さない」と書くのに CLI に阻止機構が無く、`verify` は approved イベントのキーの有無しか見ていなかった | 「散文にしか無い規約」そのもの。**doccheck を作った動機と同じ穴を、doccheck 自身が持っていた** |
| B-3 | `doccheck start plan` が「渡すもの: plan.md だけ」と言うのに、観点に「`AC` の ID 対応漏れ」を挙げる。**`AC:` 行は `tasks.md` にしかない**ので構造的に検知できない | CLI の出力と `protocol.md`「7.」の成果物表を突き合わせないと見えない |
| B-2 | 失敗時に `doccheck: <slug> / <phase>` を stdout に出してから stderr に FAIL。stdout だけ拾うと成功時と同じ 1 行に見える | 出力の分配は assert では見ていなかった |

**指示文の不備（修正済み）**: `report` を直す前に打つのか後か が散文に無い／`aidev-00-start` 手順4 に `--backlog-item` が無い（打たないと `status` の `HELD` が行単位で出ない）／design を挟まない work で `doccheck design` が要るのかが散文から判断できず、CLI のソースを読んで確認された。

**残した指摘**: `metrics --phases` に `doc_check_*` の列が無い（読み方は `protocol-analysis.md` に定義したが、列としては未提供）。母集団が貯まってから判断する。`smoke` が deliver 承認後に打つと metrics を汚す点は既存の設計上の緊張で、今回は触っていない。
